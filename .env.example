# Copy this file to .env and set values as needed

# Optional: Only if using ElevenLabs TTS (recommended for natural voice)
ELEVENLABS_API_KEY=your_api_key_here
ELEVENLABS_VOICE_ID=Rachel

# Required on Windows for pdf2image if you use the LibreOffice+PDF route
# Set to the 'bin' folder of your Poppler installation
POPPLER_PATH=C:\path\to\poppler\bin

# If LibreOffice is installed in a non-standard path, specify soffice.exe

